name: Linting Code
on: push
jobs:
  lint:
    runs-on: ubuntu-latest
    steps:
      - name: code checkout
        uses: actions/checkout@v4
      - name: install dependencies
        run: npm install
      - name: lint 
        run: npm run lint